# ── Environment ──────────────────────────────────────────────────────────────
# dev | preprod | prod
ENV=dev

# ── Database (opcional; ajustar cuando se defina el almacenamiento) ──────────
# DATABASE_URL=postgresql://arbol:arbol@localhost:5432/arbol_genealogico

# ── App ──────────────────────────────────────────────────────────────────────
# Ruta al fichero de configuración YAML (relativa a la raíz del repo)
CONFIG_PATH=config/config.yaml
